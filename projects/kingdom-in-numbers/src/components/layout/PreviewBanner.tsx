/**
 * Top-of-page banner advertising the current build state.
 * Removed for the published version; kept here so the dev preview is honest
 * about what's in flight.
 */
export function PreviewBanner() {
  return (
    <div className="preview-banner">
      <strong>v0.3 LIVE PREVIEW</strong> · 3D extruded country map · Burj Al Mamlakah hero · proportional flag bars · animated pyramid
    </div>
  );
}
